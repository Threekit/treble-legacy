import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { UploadArea } from './index';

export const Template = args => <UploadArea {...args} />;

<Meta title="Form Components/Upload Area" />

# Upload Area

### Overview

A Form Component that works with the image upload attribute type

<Canvas>
  <Story name="Upload Area" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
