import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Upload } from './index';

export const Template = args => <Upload {...args} />;

<Meta title="Form Components/Upload" />

# Upload

### Upload

A Form Component that works with the image upload attribute type

<Canvas>
  <Story name="Default" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
