import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { TotalPrice } from './index';
import ThreekitProvider from '../ThreekitProvider/index';

export const Template = args => (
  <ThreekitProvider>
    <TotalPrice {...args} />
  </ThreekitProvider>
);

<Meta title="Display/Total Price" />

# TotalPrice

### Display the Configuration's Total Price

This component will display the total price of the configuration.

Passing a string/number into the `price` prop will display that
value instead.

<Canvas>
  <Story
    name="Total Price"
    args={{
      price: '$500',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={TotalPrice} />
