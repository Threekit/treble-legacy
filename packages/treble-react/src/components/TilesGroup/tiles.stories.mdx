import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { TilesGroup } from './index';

<Meta title="Form Components/Tiles Group" />

export const Template = args => <TilesGroup {...args} />;

# Tiles Group

### A Form Component for **Asset / Part Reference** and **String** type Attributes

<Canvas>
  <Story
    name="Tiles Group"
    args={{
      title: 'Tiles Attribute Title',
      description: 'This is the description for the Tiles Group attribute.',
      value: 'arsenal',
      options: [
        {
          name: 'Celtic',
          value: 'celtic',
        },
        {
          name: 'Arsenal',
          value: 'arsenal',
        },
        {
          name: 'Dortmund',
          value: 'dortmund',
        },
        {
          name: 'Ajax',
          value: 'ajax',
        },
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## When to Use

`<TilesGroup />` are a Form Component that can be used with an **Asset / Part Reference** or
**String** type Attribute.

**TilesGroup** are great for displaying very small option sets, example a selection between
two values.

## Overview

The `<TilesGroup />` component will render our a single row of Tile components, where each Tile
represents an option in the options array.

## How to Use

### Default use with an Attribute

To connect and use the `<TilesGroup />` component with an Attribute by default, you need to pass the attribute's name
to TilesGroup component as the prop `attribute`.

E.g. `<TilesGroup attribute="Attribute Name" />`

### Custom use

You may want to use the **TilesGroup** component in a custom way rather than directly
with an attribute. To do this, instead of passing in an `attribute` prop, you
can directly pass in the `value`, `options` and `onClick` props. You can find more
information about the props in the Props Table below.

**Note**: The component will not manage your selection state, and will require
the selected value be passed in as well.

## Props

<ArgsTable of={TilesGroup} />
