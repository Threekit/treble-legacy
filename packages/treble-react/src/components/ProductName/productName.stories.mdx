import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { ProductName } from './index';
import ThreekitProvider from '../ThreekitProvider/index';

export const Template = args => (
  <ThreekitProvider>
    <ProductName {...args} />
  </ThreekitProvider>
);

<Meta title="Display/Product Name" />

# ProductName

### Display the Product's Name

The `<ProductName />` component will display the title of the Product used to
initialize the Threekit Player.

It's also possible to display a custom title by passing a string into the `title` prop.

## Demo

<Canvas>
  <Story
    name="Product Name"
    args={{
      title: 'Product Title',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={ProductName} />
