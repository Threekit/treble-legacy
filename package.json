{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "demos/*"
  ],
  "scripts": {
    "install": "lerna bootstrap",
    "start": "lerna run start",
    "format": "prettier --write ./packages ./docusaurus ./demos"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
